/*
package methodrefs;

public class Count<T extends Number> {

    public double getMin(T[] arr) {
        double min = arr[0].doubleValue();
        for(double d: (Double[]) arr) {
            if (d < min) {
                min = d;
            }
        }
        return min;
    }
}
*/
